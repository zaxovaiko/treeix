import type { Monaco } from './setup'

// Task 6 fills this in: hover, completion, signature help and diagnostics backed by window.api's language service
export function registerTypeScriptProviders(_monaco: Monaco): void {}
